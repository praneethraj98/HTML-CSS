Web Safe Fonts (WSF) - available in all browsers

Sans Serif - default font used

If desired font not present in WSF, we use Google Fonts

fonts.google.com -> search for desired font -> copy the link tag -> paste the link tag inside head

span can be used to make specific parts different